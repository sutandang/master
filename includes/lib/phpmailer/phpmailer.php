<?php  if ( ! defined('_VALID_BBC')) exit('No direct script access allowed');

require_once(_LIB.'phpmailer/class.phpmailer.php');
#$mail = new PHPMailer();
#$mail->IsMail();
/*
$mail->IsSMTP(); // telling the class to use SMTP
$mail->Host 	= "idwebhost.com"; // SMTP server
$mail->SMTPAuth = true;
// turn on SMTP authentication
$mail->Username = "sales@idwebhost.com";  // SMTP username
$mail->Password = "M0nalisa"; // SMTP password
*/


/*
$mail->From     = "$from";
$mail->FromName = "$from_name";

$mail->AddAddress( $data['order_email'] );
if ( !empty( $data['order_email_2'] ) )
	$mail->AddCC( $data['order_email_2'] );

$mail->AddCC($from);
$mail->Subject 	= $subject;
$mail->Body 	= $content;
//$mail->WordWrap = 50;

$mail->Send();
$mail->ClearAddresses();
*/
?>
